// Con EM6 podemos crear cadenas de texto que tengan variables en formato ${variable} que apunten a su contenido.

const nombre = "Angel Zarza";
const edad = 29;
const pais = "España";

console.log(`La persona es ${nombre}, tiene ${edad} y es de ${pais}`);